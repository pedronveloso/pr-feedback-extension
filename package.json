{
  "name": "pr-feedback-extension",
  "version": "0.1.8",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && node scripts/verify-build.mjs",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.317",
    "@types/node": "^22.15.30",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.3",
    "vite": "^6.2.1",
    "vitest": "^4.1.0"
  }
}
